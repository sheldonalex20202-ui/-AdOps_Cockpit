services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: adops_cockpit
    ports:
      - "5432:5432"
    volumes:
      - adops_pg:/var/lib/postgresql/data

volumes:
  adops_pg:
